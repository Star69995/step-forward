# חוקי עבודה על הפרויקט — צעד קדימה

מסמך זה מגדיר את כללי העבודה הקבועים על קוד הפרויקט. יש לפעול לפיו בכל שינוי, ולעדכן אותו כשמתבצע שינוי משמעותי במבנה, בעיצוב או בארכיטקטורה של האתר — כדי שהחוקים תמיד ישקפו את המצב בפועל.

## הקשר הפרויקט

אפליקציית React‏ (Vite) לטובת מילוי, שמירה וייצוא תוכנית אישית לקידום מטרות, המבוססת על Firebase (Auth + Firestore). האתר אוסף ומאחסן **מידע אישי רגיש** של משתמשים (תוכניות שיקום אישיות, פרטי משתמש) — כל החלטת עיצוב וקוד צריכה להתייחס לרגישות הזו כברירת מחדל.

## שימוש חוזר ברכיבים

- לפני יצירת רכיב, עמוד או פונקציית עזר חדשים — יש לבדוק קודם ב-[src/components/](src/components/), [src/services/](src/services/) ו-[src/context/](src/context/) אם כבר קיים רכיב/פונקציה שעושים את אותו הדבר או קרוב לזה.
- אם שני מקומות בקוד עושים דבר דומה (טופס, כפתור, כרטיס, הודעת שגיאה וכו') — יש לחלץ אותם לרכיב משותף אחד תחת `src/components/`, ולא לשכפל.
- רכיב חדש שנוצר ויש סיכוי סביר שישמש שוב (כפתורים, שדות טופס, כרטיסים, מצבי טעינה/שגיאה) — ייכתב מלכתחילה כרכיב גנרי (props להתאמה), לא כקוד מוטמע בתוך עמוד ספציפי.
- לפני הוספת ספרייה חיצונית חדשה לרכיב UI — לבדוק אם ניתן להשיג את אותה תוצאה עם מה שכבר קיים בפרויקט (Tailwind, react-hook-form, react-toastify).

## עיצוב אחיד — מקור אמת יחיד

- כל ערכי העיצוב הגלובליים (צבעים, גופנים, מרווחים חוזרים) חייבים להיות מוגדרים במקום אחד — [tailwind.config.js](tailwind.config.js) (ה-`theme.extend`) — ולא "קשיחים" (hard-coded) בתוך קומפוננטות בודדות. שימוש בקוד ב-`primary`, `secondary`, `success` וכדומה, לא בקודי צבע גולמיים (`#667eea` וכו') מפוזרים בקבצים.
- אם צריך צבע/גופן/רדיוס חדש שחוזר ביותר ממקום אחד — מוסיפים אותו ל-`tailwind.config.js` ולא מעתיקים ערך גולמי בין קבצים.
- עיצוב חוזר של אלמנט (כותרת סקשן, כרטיס טופס, מצב שגיאה) עובר לרכיב משותף (למשל [FormSection.jsx](src/components/FormSection.jsx)) כדי שכל שינוי עיצובי עתידי יתבצע במקום אחד ולא ידרוש מעבר על כל העמודים.
- אין להוסיף ספריית עיצוב/CSS נוספת (למשל חזרה ל-Bootstrap) לצד Tailwind — יש מקור עיצוב אחד בפרויקט.

## פתרון בעיות מהשורש, לא נקודתי

- כשמתגלה תקלה או באג, יש לאתר ולתקן את הגורם השורשי, ולא רק את התסמין הנקודתי שבו הוא התגלה.
- אם באג/חוסר עקביות חוזרים במספר מקומות (למשל טיפול שגיאות בטופס, ולידציה, מצב טעינה) — הפתרון הוא לתקן את הרכיב/פונקציה המשותפים שכולם מסתמכים עליהם, כולל בדיקה אם אותה בעיה קיימת גם במקומות אחרים בקוד שלא דווחו.
- אין "טלאים" (patches) זמניים או תיקונים שמסתירים תקלה רק במסך מסוים — כל תיקון עובר דרך המקור המשותף (רכיב, שירות ב-`src/services/`, או הגדרה גלובלית).

## שפה עברית נייטרלית

- כל טקסט פונה למשתמש (ממשק, הודעות שגיאה/הצלחה, placeholder-ים, תוויות) ייכתב בעברית נייטרלית מגדרית ככל האפשר (למשל "יש למלא שדה זה" ולא "עליך למלא"; "התחברות" ולא "התחבר/י" כשניתן לנסח בצורה ניטרלית).
- כשניסוח ניטרלי מלא לא אפשרי מבחינה תחבירית, עדיפה פנייה בלשון רבים/גוף שני נייטרלי על פני פנייה ממוגדרת.

## איקונים, לא אמוג'ים

- אין להשתמש באמוג'ים בממשק המשתמש, בקוד, בהודעות commit או בתיעוד הפרויקט (כולל README).
- במקום זאת יש להשתמש בספריית אייקונים אחידה (SVG). בפרויקט טרם קיימת ספריית אייקונים מותקנת בפועל — לפני הוספת אייקון ראשון יש לבחור ספרייה אחת (למשל lucide-react) ולהשתמש בה בעקביות בכל האתר, ולא לערבב ספריות אייקונים שונות.

## תמיכה בנייד ובאייפד

- כל רכיב ועמוד חדשים חייבים להיבדק ולתפקד היטב במסכי מובייל ובטאבלט (iPad), לא רק בדסקטופ — כולל טפסים, כפתורי פעולה, וייצוא ה-PDF.
- יש להשתמש ב-breakpoints הסטנדרטיים של Tailwind (`sm:`, `md:`, `lg:`) ולא בפתרונות מותאמים חד-פעמיים למסך מסוים.
- לפני סגירת משימת UI, יש לוודא ידנית (או לפחות בבדיקת responsive באמולטור/דפדפן) שהעמוד תקין גם ברוחב טאבלט וגם ברוחב טלפון.

## אבטחה והצפנה

האתר מכיל מידע אישי רגיש (תוכניות שיקום אישיות של משתמשים) — אבטחה היא דרישת יסוד ולא תוספת:

- כל גישה לנתונים ב-Firestore חייבת לעבור דרך [firestore.rules](firestore.rules) המגביל קריאה/כתיבה למשתמש המאומת שהנתונים שייכים לו בלבד (`request.auth.uid == userId`) — כל שינוי בסכימת הנתונים חייב להתלוות בעדכון תואם בחוקי האבטחה.
- אין לשמור מפתחות/סודות (API keys רגישים, Service Account) בקוד הצד-לקוח או ב-repo. הגדרות Firebase הציבוריות (ב-[src/services/firebase.js](src/services/firebase.js)) מוגנות בפועל על ידי חוקי Firestore/Auth, לא על ידי הסתרתן.
- כל שדה חדש שנוסף לטופס ונשמר במסד הנתונים — יש לבחון האם מדובר במידע רגיש, ולוודא שחוקי ה-Firestore מכסים אותו.
- אימות משתמשים מתבצע אך ורק דרך Firebase Auth הקיים — אין להוסיף מנגנון אימות מקביל.
- תקשורת עם Firebase מתבצעת תמיד דרך HTTPS (ברירת המחדל של Firebase SDK) — אין לעקוף זאת.
- בהמשך הפיתוח יש לשקול הצפנה נוספת (at-rest / field-level) לשדות רגישים בתוכן התוכנית האישית, ולתעד כאן את ההחלטה כשתתקבל.

## מינימום קריאות לשרת ולבסיס הנתונים, יעילות

- כל קריאה ל-Firestore (או לכל שירות חיצוני) עולה כסף וזמן — יש לצמצם למינימום ההכרחי:
  - להימנע מקריאה חוזרת לאותם נתונים באותו טעינת עמוד/session; לשמור נתונים שכבר נטענו ב-state/context (למשל [AuthContext.jsx](src/context/AuthContext.jsx)) ולהשתמש בהם מחדש במקום לשלוף שוב.
  - שמירה אוטומטית ([CloudSaveButton.jsx](src/components/CloudSaveButton.jsx) וכל מנגנון autosave עתידי) חייבת להיות עם debounce/throttle, ולא לשלוח כתיבה לשרת על כל הקלדה או שינוי שדה בודד.
  - לשלוף רק את השדות/מסמכים שבאמת נדרשים למסך הנוכחי, לא מסמכים שלמים "ליתר ביטחון".
  - לפני הוספת קריאה חדשה לשרת, יש לבדוק אם ניתן להסתפק בנתון שכבר קיים בזיכרון/state.
- באופן כללי, כל קוד חדש (רינדור, לולאות, טיפול בטפסים) ייכתב מתוך מחשבה על יעילות — להימנע מחישובים/רינדורים מיותרים וממבני נתונים לא יעילים, ולא רק "לגרום לזה לעבוד".

## חוויית משתמש נקייה ואינטואיטיבית

- כל מסך/זרימה חדשים חייבים להיות ברורים מאליהם למשתמש ללא צורך בהסבר: פעולה עיקרית בולטת אחת למסך, ניווח/כותרות ברורים, ומצבי טעינה/שגיאה/הצלחה מפורשים (למשל דרך react-toastify הקיים) ולא שקטים.
- להעדיף פשטות על פני ריבוי אפשרויות: אם ניתן לצמצם שדות/צעדים בטופס מבלי לפגוע בתפקוד — לצמצם.
- כל פעולה הרסנית או בלתי הפיכה (מחיקת תוכנית, יציאה מהחשבון) חייבת אישור מפורש מהמשתמש לפני ביצוע.
- לשמור על עקביות בין מסכים (מיקום כפתורים, ניסוח, מבנה טופס) כדי שהמשתמש לא יצטרך ללמוד מחדש כל מסך.

## עדכון קובץ זה

כשמתבצע שינוי גדול באתר — מעבר לספריית עיצוב/אייקונים אחרת, שינוי במבנה הנתונים ב-Firestore, הוספת מנגנון אימות/הרשאות, שינוי בארכיטקטורת התיקיות — יש לעדכן את הסעיף הרלוונטי במסמך הזה כחלק מאותו שינוי, לא בנפרד.
