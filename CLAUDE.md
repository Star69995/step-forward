# חוקי עבודה על הפרויקט — צעד קדימה

מסמך זה מגדיר את כללי העבודה הקבועים על קוד הפרויקט. יש לפעול לפיו בכל שינוי, ולעדכן אותו כשמתבצע שינוי משמעותי במבנה, בעיצוב או בארכיטקטורה של האתר — כדי שהחוקים תמיד ישקפו את המצב בפועל.

## הקשר הפרויקט

אפליקציית React‏ (Vite) לטובת מילוי, שמירה וייצוא תוכנית אישית לקידום מטרות, המבוססת על Firebase (Auth + Firestore). האתר אוסף ומאחסן **מידע אישי רגיש** של משתמשים (תוכניות שיקום אישיות, פרטי משתמש) — כל החלטת עיצוב וקוד צריכה להתייחס לרגישות הזו כברירת מחדל.

לכל משתמש יש תפקיד קבוע שנבחר בהרשמה: **מקבל/ת שירות** (בעל/ת התוכנית) או **נותן/ת שירות** (מלווה תוכניות ששותפו איתו/ה). ראו את הסעיף "סוגי משתמשים, שיתוף והרשאות" למטה למודל המלא.

## שימוש חוזר ברכיבים

- לפני יצירת רכיב, עמוד או פונקציית עזר חדשים — יש לבדוק קודם ב-[src/components/ui/](src/components/ui/), [src/components/](src/components/), [src/services/](src/services/) ו-[src/context/](src/context/) אם כבר קיים רכיב/פונקציה שעושים את אותו הדבר או קרוב לזה.
- הרכיבים הגנריים המשותפים חיים ב-`src/components/ui/` ומהווים את מקור האמת היחיד למראה ולהתנהגות שלהם בכל האתר:
  - [Button.jsx](src/components/ui/Button.jsx) — כל כפתור פעולה (variant: `primary`/`success`/`danger`/`blue`/`outline`, מצב `loading` מובנה).
  - [TextField.jsx](src/components/ui/TextField.jsx) — כל שדה טקסט/טקסטאריה עם תווית ואייקון (משמש גם דרך [FormSection.jsx](src/components/FormSection.jsx) לשדות מחוברים ל-react-hook-form, וגם ישירות לשדות מבוקרים כמו ב-[Login.jsx](src/pages/Login.jsx)).
  - [Spinner.jsx](src/components/ui/Spinner.jsx) — כל אינדיקציית טעינה.
  - [ConfirmDialog.jsx](src/components/ui/ConfirmDialog.jsx) — כל אישור לפני פעולה הרסנית (לא `window.confirm`).
  - [CollapsibleSection.jsx](src/components/ui/CollapsibleSection.jsx) — כל מקטע שניתן לקפל/לפתוח (הכרטיסים הראשיים ב-[FormPage.jsx](src/pages/FormPage.jsx) וכל מטרה קצרת טווח ב-[GoalSection.jsx](src/components/GoalSection.jsx)). תומך גם בצבע קבוע (`accent`) וגם בצבע דינמי לפי instance (`accentColor`). ייצוא ה-PDF ([PDFButton.jsx](src/components/PDFButton.jsx)) מסתמך על ה-class name `collapsible-section`/`collapsible-content` כדי לפתוח הכל בכוח לפני הרינדור — אסור לשנות class names אלה בלי לעדכן גם את PDFButton ואת index.css.
  - [InfoHint.jsx](src/components/ui/InfoHint.jsx) — סימן שאלה קטן עם הסבר/שאלה מכוונת ליד שדה שהכוונה בו לא מובנת מאליה. לא כל שדה צריך אחד — רק במקומות שבאמת עלולים לבלבל.
  - [Badge.jsx](src/components/ui/Badge.jsx) — כל תג/פיל צבעוני קטן (תפקיד, היקף שיתוף, הרשאה) — כולל variant מיוחד `onDark` לשימוש על גבי הגרדיאנט הכהה של [Header.jsx](src/components/Header.jsx). [RoleBadge.jsx](src/components/RoleBadge.jsx) עוטף אותו עם המיפוי מ-[roles.js](src/services/roles.js) — לא לשכפל את תוויות/צבעי התפקידים במקום נוסף.
  - [TrashSection.jsx](src/components/ui/TrashSection.jsx) — כל "פח מחזור" של פריטים שנמחקו (תוכניות, שיתופים, הערות): מקטע מתקפל עם רשימת פריטים, "ימים למחיקה סופית", שחזור ומחיקה-לצמיתות (עם `ConfirmDialog` פנימי משלה). ראו הסעיף "מחיקה רכה ופח מחזור" למטה למודל המלא — אין לבנות מסך/רשימת "נמחקו" חדשה בלי לעבור דרך הרכיב הזה.
- הגישה לרשימת התוכניות של משתמש (fetch + מיון) ממומשת פעם אחת ב-[usePlans.js](src/services/usePlans.js) ומשמשת גם את [Profile.jsx](src/pages/Profile.jsx), את [PlanSwitcher.jsx](src/components/PlanSwitcher.jsx) וגם את [RecipientPlans.jsx](src/pages/RecipientPlans.jsx) (בצד נותן השירות, עם ה-`uid` של מקבל השירות) — אסור לממש שוב שליפת תוכניות ב-`getDocs` ישירות בקומפוננטה חדשה.
- שיתוף/הרשאות והערות ממומשים דרך [useShares.js](src/services/useShares.js) (ניהול בצד מקבל השירות), [useSharedWithMe.js](src/services/useSharedWithMe.js) (גילוי בצד נותן השירות, collection-group query), [resolveEmailToUser.js](src/services/resolveEmailToUser.js) (החיפוש היחיד — `get` לפי מייל מדויק, לא `list`), ו-[useComments.js](src/services/useComments.js) + [CommentThread.jsx](src/components/CommentThread.jsx) (יומן ההערות) — אין לממש שוב לוגיקת שיתוף/הרשאות/הערות במקום אחר; כל תוספת (שדה, מסך) שנוגעת בהן עוברת דרך הקבצים האלה.
- מחיקה של תוכנית, שיתוף או הערה היא תמיד מחיקה רכה דרך [trash.js](src/services/trash.js) — ראו הסעיף "מחיקה רכה ופח מחזור" למטה למודל המלא.
- לפני הוספת כפתור/שדה/מודאל חדש — יש להשתמש ברכיבים אלה עם ה-props הקיימים; אם חסר variant, מוסיפים אותו לרכיב המשותף ולא יוצרים כפתור עצמאי.
- רכיב חדש שנוצר ויש סיכוי סביר שישמש שוב — ייכתב מלכתחילה כרכיב גנרי תחת `src/components/ui/` (props להתאמה), לא כקוד מוטמע בתוך עמוד ספציפי.
- לפני הוספת ספרייה חיצונית חדשה לרכיב UI — לבדוק אם ניתן להשיג את אותה תוצאה עם מה שכבר קיים בפרויקט (Tailwind, react-hook-form, react-toastify, lucide-react).

## עיצוב אחיד — מקור אמת יחיד

- הפרויקט על Tailwind CSS v4. כל ערכי העיצוב הגלובליים (צבעים, גופנים, מרווחים חוזרים) חייבים להיות מוגדרים במקום אחד — בבלוק ה-`@theme` שבראש [index.css](src/index.css) — ולא "קשיחים" (hard-coded) בתוך קומפוננטות בודדות. אין קובץ `tailwind.config.js` בפרויקט (הוסר עם המעבר ל-v4) — לא ליצור אותו מחדש. הצבעים המוגדרים כרגע: `primary`, `secondary`, `success`, `info`, `warning`, `danger`, וכן `headerFrom`/`headerTo` (גרדיאנט הסרגל העליון בלבד — כחול-סגול, נפרד מ-`primary`/`secondary` הכהים ששאר האתר משתמש בהם). בנוסף, טוקני נייטרל סמנטיים למצב תצוגה (ראו סעיף מצב תצוגה למטה): `bg-page`, `surface`, `surface-muted`, `heading`, `body`, `muted`, `border`. שימוש בקוד בשמות אלה (כ-`bg-primary`/`text-danger`/`bg-surface`/`text-heading` וכו' ב-className, או `var(--color-primary)` וכו' ב-JS/inline style), לא בקודי צבע גולמיים (`#667eea` וכו') ולא במחלקות הצבע הבררת-מחדל של Tailwind (`bg-white`, `text-gray-800`, `border-gray-200` וכו') המפוזרות בקבצים — כל "לבן"/"אפור" בממשק הוא בהכרח אחד מטוקני הנייטרל שלמעלה, כדי שיתהפך נכון במצב כהה.
- אין להוסיף `style={{ fontFamily: ... }}` ידני בשום רכיב — הגופן (`Rubik`) מוגדר גלובלית פעם אחת באותו בלוק `@theme` ב-[index.css](src/index.css) (כ-`--font-rubik`, נגיש ב-Tailwind כ-`font-rubik`).
- אם צריך צבע/גופן/רדיוס חדש שחוזר ביותר ממקום אחד — מוסיפים אותו לבלוק `@theme` ב-`index.css` ולא מעתיקים ערך גולמי בין קבצים. במקומות בהם נדרש צבע דינמי לפי instance ב-JS (למשל הצבע המשתנה של [Goal.jsx](src/components/Goal.jsx)/[GoalSection.jsx](src/components/GoalSection.jsx)) — יש להשתמש ב-custom property של הצבע (`"var(--color-primary)"` וכו', ראו המיפוי ב-GoalSection.jsx) ולא לשכפל את ערך ה-hex במקום נוסף.
- אפקטי hover/focus (הגבהה, צל, מסגרת) ממומשים באמצעות מחלקות Tailwind (`hover:`, `focus:`, `active:`) או ה-class המשותף `.accent-field` ב-`index.css` לשדות עם צבע דינמי — **לא** באמצעות `onMouseEnter`/`onMouseLeave`/`onFocus`/`onBlur` שמשנים `style` ב-JS. מימוש כזה משוכפל בכל קובץ בנפרד, לא עובד במגע (מובייל/טאבלט), וסותר את עקרון המקור האחד.
- עיצוב חוזר של אלמנט (כותרת סקשן, כרטיס טופס, מצב שגיאה) עובר לרכיב משותף (ראו סעיף "שימוש חוזר ברכיבים" למעלה) כדי שכל שינוי עיצובי עתידי יתבצע במקום אחד ולא ידרוש מעבר על כל העמודים.
- אין להוסיף ספריית עיצוב/CSS נוספת (למשל Bootstrap) לצד Tailwind — יש מקור עיצוב אחד בפרויקט.
- **צפיפות תצוגה (מרווח/קומפקטי):** האתר תומך בשני מצבי מרווח גלובליים, נשמרים בפרופיל המשתמש (`users/{uid}.density`, דרך [userProfile.js](src/services/userProfile.js) ו-[AuthContext.jsx](src/context/AuthContext.jsx)) ומוחלים על כל האתר דרך מאפיין `data-density` על ה-`<html>`. ערכי המרווח של שני המצבים מוגדרים **פעם אחת** ב-[index.css](src/index.css): ברירת המחדל ("מרווח") בבלוק `@theme` (משתני `--space-*`), והדריסה למצב "קומפקטי" בכלל `:root[data-density="compact"]` מיד אחריו. קומפוננטות צורכות את הערכים דרך תחביר arbitrary-value של Tailwind (`p-[var(--space-card-pad)]` וכו') — לעולם לא ערך מרווח קשיח כפול, ולא לוגיקת צפיפות בצד ה-JS של קומפוננטה בודדת. הבקר להחלפת המצב הוא [SegmentedToggle.jsx](src/components/ui/SegmentedToggle.jsx) (רכיב גנרי, לא ייעודי לצפיפות), מוצג ב-[Profile.jsx](src/pages/Profile.jsx). לא כל מרווח בקוד הופך לטוקן — מרווחים זעירים (ריפוד תגים/tooltip, מרווחי אייקון-לתווית) נשארים קבועים בכוונה כדי לא לפגוע בקריאות; טוקן חדש נוסף רק למרווח שחוזר על עצמו לאורך הדף ותורם בפועל לתחושת "מרווח מדי".
- **מצב תצוגה (בהיר/כהה/אוטומטי):** אותה שיטה בדיוק כמו צפיפות תצוגה, עם מנגנון נוסף למניעת הבהוב (FOUC) כי בשונה מצפיפות, יש כאן ברירת מחדל "אוטומטי" שתלויה ב-`prefers-color-scheme` של הדפדפן/מערכת ההפעלה. הטוקנים הנייטרליים (`bg-page`, `surface`, `surface-muted`, `heading`, `body`, `muted`, `border`, וגם `primary`/`secondary` עם גוון בהיר יותר לקריאות) מוגדרים ב-[index.css](src/index.css): ברירת המחדל הבהירה בבלוק `@theme`, והדריסה למצב כהה בכלל `:root[data-theme="dark"]` מיד אחריו. **אין** שימוש ב-`dark:` variant של Tailwind בפרויקט — קומפוננטות תמיד משתמשות במחלקה הסמנטית הרגילה (`bg-surface`, `text-heading` וכו'), והצבע בפועל מוחלף לגמרי דרך ה-CSS custom property, בלי תוספת מחלקה ובלי לוגיקת theme בצד ה-JS של קומפוננטה בודדת. ההעדפה (`'light' | 'dark' | 'auto'`) נשמרת ב-`users/{uid}.themeMode` (כמו density) אבל גם ב-`localStorage` (מפתח `themeMode`) — כי בשונה מצפיפות, המצב חייב להיות נכון גם למשתמש לא מחובר/לפני טעינת הפרופיל, כדי שלא יהיה הבהוב של ערכת צבעים שגויה ברגע הראשון. סקריפט inline קטן ב-[index.html](index.html) קורא את אותו מפתח `localStorage` וקובע `data-theme` על ה-`<html>` **לפני** הרינדור הראשון של React; [AuthContext.jsx](src/context/AuthContext.jsx) ממשיך מאותה נקודה (`themeMode`/`resolvedTheme`/`setThemeMode`), כולל פתרון "אוטומטי" דרך `window.matchMedia('(prefers-color-scheme: dark)')` עם מאזין לשינוי חי. הבקר הוא אותו [SegmentedToggle.jsx](src/components/ui/SegmentedToggle.jsx) הגנרי, ליד בקר הצפיפות ב-[Profile.jsx](src/pages/Profile.jsx). ייצוא ה-PDF ([PDFButton.jsx](src/components/PDFButton.jsx)) כופה `data-theme="light"` זמנית בזמן הלכידה (ומחזיר את המצב הקודם ב-`restore()`) — מסמך מיוצא תמיד נראה כמו מסמך בהיר, בלי קשר למצב הפעיל של הצופה.

## פתרון בעיות מהשורש, לא נקודתי

- כשמתגלה תקלה או באג, יש לאתר ולתקן את הגורם השורשי, ולא רק את התסמין הנקודתי שבו הוא התגלה.
- אם באג/חוסר עקביות חוזרים במספר מקומות (למשל טיפול שגיאות בטופס, ולידציה, מצב טעינה) — הפתרון הוא לתקן את הרכיב/פונקציה המשותפים שכולם מסתמכים עליהם, כולל בדיקה אם אותה בעיה קיימת גם במקומות אחרים בקוד שלא דווחו.
- אין "טלאים" (patches) זמניים או תיקונים שמסתירים תקלה רק במסך מסוים — כל תיקון עובר דרך המקור המשותף (רכיב, שירות ב-`src/services/`, או הגדרה גלובלית).

## שפה עברית נייטרלית

- כל טקסט פונה למשתמש (ממשק, הודעות שגיאה/הצלחה, placeholder-ים, תוויות) ייכתב בעברית נייטרלית מגדרית ככל האפשר (למשל "יש למלא שדה זה" ולא "עליך למלא"; "התחברות" ולא "התחבר/י" כשניתן לנסח בצורה ניטרלית).
- כשניסוח ניטרלי מלא לא אפשרי מבחינה תחבירית, עדיפה פנייה בלשון רבים/גוף שני נייטרלי על פני פנייה ממוגדרת.

## איקונים, לא אמוג'ים

- אין להשתמש באמוג'ים בממשק המשתמש, בקוד, בהודעות commit או בתיעוד הפרויקט (כולל README).
- ספריית האייקונים הרשמית והיחידה בפרויקט היא [lucide-react](https://lucide.dev/) — יש להשתמש בה בלבד לכל אייקון חדש, ולא לערבב ספריות אייקונים נוספות.
- הודעות `toast` (react-toastify) לא צריכות אימוג'י/אייקון בתוך הטקסט — ל-`toast.success`/`toast.error` יש כבר אייקון מובנה משלהן; טקסט ההודעה צריך להיות נקי.

## תמיכה בנייד ובאייפד

- כל רכיב ועמוד חדשים חייבים להיבדק ולתפקד היטב במסכי מובייל ובטאבלט (iPad), לא רק בדסקטופ — כולל טפסים, כפתורי פעולה, וייצוא ה-PDF.
- יש להשתמש ב-breakpoints הסטנדרטיים של Tailwind (`sm:`, `md:`, `lg:`) ולא בפתרונות מותאמים חד-פעמיים למסך מסוים.
- לפני סגירת משימת UI, יש לוודא ידנית (או לפחות בבדיקת responsive באמולטור/דפדפן) שהעמוד תקין גם ברוחב טאבלט וגם ברוחב טלפון.

## PWA ואייקוני האתר

האתר הוא PWA מלא (ניתן להתקנה על מסך הבית באנדרואיד ובאייפון, עובד גם במצב לא-מקוון חלקי):

- ה-manifest (שם, צבעים, אייקונים, `display: standalone`, `dir: rtl`) מוגדר במלואו כאובייקט `manifest` בתוך ה-plugin `VitePWA` ב-[vite.config.js](vite.config.js) — **אין** קובץ `public/manifest.json` נפרד; `dist/manifest.webmanifest` נוצר אוטומטית מתוך ההגדרה הזו בזמן `build`. רישום ה-service worker מתבצע ידנית ב-[main.jsx](src/main.jsx) (`registerSW` מ-`virtual:pwa-register`), ולכן אין להוסיף גם `injectRegister` אוטומטי — זה ייצור רישום כפול.
- כל אייקוני האתר (favicon, אייקוני PWA לאנדרואיד בגדלים 64/192/512, אייקון maskable ל-512, ו-apple-touch-icon 180x180 לאייפון) נוצרים **פעם אחת ממקור יחיד** — [public/favicon.svg](public/favicon.svg) — על ידי `npm run pwa:assets` (ראו [pwa-assets.config.js](pwa-assets.config.js), חבילת `@vite-pwa/assets-generator`). כדי לשנות את הלוגו/אייקון האתר: עורכים רק את `favicon.svg` ומריצים מחדש את הפקודה — **אסור** להוסיף/לערוך קובצי PNG של אייקונים ידנית בתיקיית `public/`, וגם לא ליצור מקור SVG נוסף במקום אחר.
- הוחלט **לא** לייצר מסכי פתיחה (splash screens) של אפל (`combinePresetAndAppleSplashScreens`) — אפשרות שקיימת בכלי היצירה אך מייצרת כ-70 קובצי PNG (מעל 5MB) עבור כל שילוב מכשיר/כיוון/מצב תצוגה, תוספת משקל שלא מצדיקה את עצמה בפרויקט בגודל הזה. במקומם, אייפון מציג מסך פתיחה פשוט לפי `background_color`/`theme_color` שבמניפסט. יש לשקול מחדש רק אם תהיה דרישת מוצר מפורשת למסכי פתיחה מותאמים.
- לא להשתמש ב-`dark:`/`prefers-color-scheme` בתוך ה-manifest או האייקונים עצמם — האייקון הוא אחיד לשני המצבים (בהיר/כהה), עקבי עם עיצוב הגרדיאנט הקבוע של הכותרת (ראו סעיף מצב תצוגה למעלה).

## סוגי משתמשים, שיתוף והרשאות

- לכל משתמש יש `role` קבוע שנקבע בהרשמה ([Register.jsx](src/pages/Register.jsx)) ונשמר במסמך `users/{uid}` (ראו [userProfile.js](src/services/userProfile.js)): `recipient` (מקבל/ת שירות, בעל/ת התוכניות) או `provider` (נותן/ת שירות, מלווה תוכניות ששותפו). התפקיד לא ניתן לשינוי עצמי לאחר ההרשמה (גם ברמת [firestore.rules](firestore.rules)) — אין להוסיף מסך "שינוי תפקיד" בלי לעדכן גם את החוק המתאים.
- לכל משתמש (גם אם נרשם עם מייל) יש גם `username` קבוע וייחודי, נבחר בהרשמה ונשמר גם על הפרופיל וגם כ-`usernameIndex/{username}` (ראו [usernameIndex.js](src/services/usernameIndex.js), `claimUsername`) — כמו התפקיד, גם השם לא ניתן לשינוי לאחר קביעתו (אין תכונת "שינוי שם משתמש"). בשונה מ-`emailIndex`, ה-`get` על `usernameIndex` **פומבי ולא דורש התחברות** במכוון (שם משתמש הוא כינוי ציבורי מיועד-לשיתוף, לא PII כמו מייל) — מה שמאפשר גם בדיקת זמינות חיה בזמן הרשמה — אבל המסמך שם **אסור** שיכיל `displayName` או כל מידע מזהה נוסף, בדיוק בגלל שהוא פומבי. משתמשים קיימים-מלפני-התכונה (בלי `username` בפרופיל) מנותבים אוטומטית למסך השלמת הרשמה ([Register.jsx](src/pages/Register.jsx)) בהתחברות הבאה שלהם, דרך אותו מנגנון שמטפל במשתמש Google חדש-לגמרי.
- **חשבון אנונימי (שם משתמש + סיסמה, ללא מייל):** אפשרות הרשמה נוספת (לצד מייל/סיסמה ו-Google) שאינה דורשת ואינה שומרת מייל אמיתי — עם אזהרת טקסט מפורשת (ולא קוד גיבוי) שהחשבון לא ניתן לשחזור בשום צורה אם הסיסמה נשכחת. ממומש **בלי** מנגנון אימות מקביל: נעשה שימוש ב-Firebase Auth email/password הרגיל, עם מייל סינתטי לא-נגיש שנגזר דטרמיניסטית משם המשתמש (`${username}@usernames.invalid`, דומיין `.invalid` שמור לפי RFC 2606 — ראו [anonymousAccount.js](src/services/anonymousAccount.js)). בזכות הדטרמיניזם, התחברות בשם משתמש ([Login.jsx](src/pages/Login.jsx)) לא דורשת שום קריאה ל-Firestore — רק חישוב מחדש של אותו מייל סינתטי. הדגל `users/{uid}.isAnonymous` קובע: אין `emailIndex` (לעולם לא מאומת), ואין שדה "שם מלא" נשמר בשום מקום (ראו הבהרה למטה) — כל מקום בקוד שמציג `displayName`/`email` של משתמש (הדר, הערות, שיתופים, ייחוס גרסה) **חייב** לעבור דרך `formatUserLabel` ([userProfile.js](src/services/userProfile.js)), שלעולם לא נופל בחזרה למייל סינתטי. אפשר להמיר חשבון בין רגיל לאנונימי ובחזרה בעמוד הפרופיל ([Profile.jsx](src/pages/Profile.jsx), דרך [accountConversion.js](src/services/accountConversion.js)) — דורש re-authentication בסיסמה נוכחית ומוגבל לחשבונות מייל/סיסמה בלבד (חשבון Google-בלבד לא נתמך להמרה); שם המשתמש עצמו לעולם לא משתנה בהמרה.
- שדה "שם מלא" של תוכנית (ב-[FormPage.jsx](src/pages/FormPage.jsx)) הוא שדה תוכן של התוכנית עצמה, לא שדה פרופיל — עבור תוכנית ששייכת לחשבון אנונימי הוא **לא נרשם** בכלל ב-react-hook-form (כך שלעולם לא נשמר ל-Firestore, גם אם נטען ערך ישן ממנו לפני שהחשבון הפך לאנונימי — ראו ה-guard על כך ב-`applyData`/בשמירה האוטומטית), ובמקומו מוצגת הערה שהשם ניתן להוספה **רק** באופן חד-פעמי בזמן ייצוא ל-PDF (דרך `onBeforeExport` ב-[PDFButton.jsx](src/components/PDFButton.jsx) ו-`PromptDialog` ב-[FormPage.jsx](src/pages/FormPage.jsx)) — הערך מוזרם ישירות ל-DOM element הלא-מבוקר ממש לפני הצילום, לא דרך state של הטופס.
- שיתוף נעשה לפי כתובת מייל מדויקת **או** שם משתמש מדויק — אין ולא יתווסף מנגנון חיפוש/דירקטורי חופשי (גם `usernameIndex` נגיש רק ב-`get` מדויק, לעולם לא `list`). הפענוח היחיד ממייל ל-uid הוא [resolveEmailToUser.js](src/services/resolveEmailToUser.js) (`get` על `emailIndex/{email}`); הפענוח המקביל משם משתמש הוא [resolveUsernameToUser.js](src/services/resolveUsernameToUser.js) (`get` על `usernameIndex/{username}`) — [Providers.jsx](src/pages/Providers.jsx) בוחר ביניהם לפי נוכחות `@` בטקסט שהוזן. מסמך ה-`emailIndex` נוצר רק כש-`email_verified === true` וכשה-uid/מייל תואמים לטוקן המאומת של המשתמש עצמו — כדי שאי אפשר "לתפוס" מייל של מישהו אחר.
- מקבל שירות מנהל את נותני השירות שלו במסך "נותני שירות" ([Providers.jsx](src/pages/Providers.jsx), שם נבחר בכוונה כדי לא להתנגש עם שדה הטקסט החופשי "שותפים" שכבר קיים בתוך הטופס עצמו). כל שיתוף הוא מסמך ב-`users/{recipientUid}/shares/{providerUid}` (דרך [useShares.js](src/services/useShares.js)) עם `scope` (`all`/`selected` + `planIds`) ו-`permission` (`view`/`edit`) — המקבל יכול לבטל בכל רגע (מחיקה רכה, ראו "מחיקה רכה ופח מחזור" למטה), וזה משפיע באופן מיידי כי [firestore.rules](firestore.rules) מתייחס לשיתוף עם `deletedAt` מוגדר כלא-קיים (`shareActive`) בכל קריאה/כתיבה לתוכנית, לא רק בזמן הצגת הרשימה. תווית התצוגה של כל צד בשיתוף (`providerLabel`/`recipientLabel`) מחושבת פעם אחת בזמן היצירה דרך `formatUserLabel` ונשמרת על מסמך השיתוף — לא מחושבת מחדש בכל מסך תצוגה בנפרד.
- נותן שירות מגלה את מקבלי השירות ששיתפו איתו דרך שאילתת collection-group על `shares` ([useSharedWithMe.js](src/services/useSharedWithMe.js), עם [ProviderSwitcher.jsx](src/components/ProviderSwitcher.jsx) בסרגל העליון ו-[RecipientPlans.jsx](src/pages/RecipientPlans.jsx) לרשימת התוכניות) — כל שינוי במבנה מסמך השיתוף חייב להתלוות בעדכון של ה-`fieldOverrides` המתאים ב-[firestore.indexes.json](firestore.indexes.json). שאילתת collection-group **חייבת** בלוק `match` נפרד עם `{path=**}` ב-[firestore.rules](firestore.rules) (מעבר לבלוק ה-`match` הספציפי של הנתיב הרגיל) — בלוק ספציפי לבדו, ולו הזהה מבחינה לוגית, לא נבדק כלל עבור שאילתת collection-group ונכשל עם "No matching allow statements" (התגלה ואומת מול האמולטור המקומי).
- [FormPage.jsx](src/pages/FormPage.jsx) מקבל `ownerUid` אופציונלי ב-query string (בנוסף ל-`planId`) כדי שנותן שירות יוכל לפתוח תוכנית של מקבל שירות באותו עמוד; ההרשאה בפועל (`canEdit`) נשלפת מחדש בכל טעינה (לא מסתמכת על state ניווט) כדי שביטול/הורדת הרשאה תיכנס לתוקף באופן מיידי. צפייה-בלבד נועלת גם את תיבות סימון ההשלמה (לא רק את שדות התוכן) — סימון התקדמות הוא כתיבה לתוכנית ומחייב הרשאת `edit`.
- הערות התקדמות ([useComments.js](src/services/useComments.js) + [CommentThread.jsx](src/components/CommentThread.jsx)) הן יומן נפרד מתוכן התוכנית עצמו — ניתן לצרף אותן לתוכנית כולה, למטרה קצרת-טווח ספציפית, או למקטע "הכנה לתהליך" (`targetGoal` הוא מחרוזת מפתח גנרית, לא בהכרח מטרה — כל מקטע חדש שירצה הערות משלו יכול להשתמש באותו מנגנון עם מפתח ייחודי משלו, ללא צורך בשינוי מודל הנתונים), כל משתמש עם גישה לתוכנית (גם צפייה-בלבד) יכול לכתוב הערה, אך רק מקבל השירות (בעל התוכנית) או כותב ההערה עצמו יכולים למחוק (מחיקה רכה) או לשחזר אותה. ההערות מסומנות `pdf-hidden` בכוונה — הן לא חלק מייצוא ה-PDF.

## אבטחה והצפנה

האתר מכיל מידע אישי רגיש (תוכניות שיקום אישיות של משתמשים) — אבטחה היא דרישת יסוד ולא תוספת:

- כל גישה לנתונים ב-Firestore חייבת לעבור דרך [firestore.rules](firestore.rules). מעבר לחוק הבסיסי של תוכנית בעלים בלבד (`request.auth.uid == userId`), הקובץ מגדיר גם את הגישה המורשית של נותני שירות לתוכניות ששותפו (ראו הסעיף "סוגי משתמשים, שיתוף והרשאות" למעלה) ואת חוקי `users/{uid}`, `emailIndex` ו-`shares` — כל שינוי בסכימת הנתונים חייב להתלוות בעדכון תואם בחוקי האבטחה, ולא להסתמך על בדיקות בצד הלקוח בלבד (אין Cloud Functions בפרויקט — `firestore.rules` הוא קו ההגנה היחיד).
- **כל שינוי ב-`firestore.rules` חייב להיבדק מול האמולטור המקומי לפני פריסה** — `npm run emulators` + `npm run verify:rules` (ראו README.md). זו לא המלצה גרפית: חוקים שנראו נכונים "על הנייר" נכשלו בפועל פעמיים במהלך הפיתוח של מודל השיתוף (כלל `write` על מחיקת שיתוף שנשבר כי הוא הסתמך על `request.resource.data` שלא קיים בפעולת `delete`, ושאילתת collection-group שנדחתה לגמרי כי חסר בלוק `match /{path=**}/...` נפרד מהנתיב הספציפי) — שתי התקלות התגלו ותוקנו רק כי נבדקו בפועל מול האמולטור, לא רק נקראו.
  - `firebase.json` מגדיר `singleProjectMode: true`, שנועל את אמולטור ה-Auth לפרויקט אחד בלבד — לכן פקודת ה-`npm run emulators` ב-`package.json` **חייבת** לכלול `--project demo-step-forward` במפורש (לא להסתמך על ברירת המחדל של `.firebaserc`, שמצביעה על הפרויקט האמיתי `beyond-borders-23adb`), כדי להתאים גם ל-`.env.emulator` (`dev:emulator`) וגם ל-[verify-firestore-rules.mjs](scripts/verify-firestore-rules.mjs) ששניהם מניחים פרויקט דמו בשם `demo-step-forward`. חוסר התאמה כאן גורם לאימות המייל בסקריפט הבדיקה להיכשל בשקט (oobCode נוצר לפרויקט הלא-נכון) — התגלה ותוקן במהלך פיתוח מודל המחיקה הרכה.
- אין לשמור מפתחות/סודות (API keys רגישים, Service Account) בקוד הצד-לקוח או ב-repo. הגדרות Firebase (ב-[src/services/firebase.js](src/services/firebase.js)) נטענות ממשתני סביבה (`import.meta.env.VITE_...`) המוגדרים ב-`.env` מקומי (לא ב-git, ראו `.gitignore`) — לפי התבנית ב-[.env.example](.env.example). ערכי ה-API הללו עדיין אינם סוד קריטי בפועל (מוגנים ע"י חוקי Firestore/Auth ולא ע"י הסתרתם), אך אין לחזור ולקודד אותם בקוד המקור.
- כל שדה חדש שנוסף לטופס ונשמר במסד הנתונים — יש לבחון האם מדובר במידע רגיש, ולוודא שחוקי ה-Firestore מכסים אותו.
- אימות משתמשים מתבצע אך ורק דרך Firebase Auth הקיים — אין להוסיף מנגנון אימות מקביל.
- תקשורת עם Firebase מתבצעת תמיד דרך HTTPS (ברירת המחדל של Firebase SDK) — אין לעקוף זאת.
- **הצפנה (החלטה מתועדת, 2026-07-17):** הוחלט **לא** להוסיף הצפנת client-side/end-to-end לתוכן התוכניות בשלב הזה. הנימוק: Firestore כבר מצפין את כל הנתונים at-rest (מפתחות מנוהלים של Google) וחוקי ה-Firestore כבר מגבילים גישה לבעל הנתונים בלבד — זה מכסה את רוב האיומים הרלוונטיים (משתמשים אחרים, תקיפת רשת, גניבת דיסק פיזי). ההגנה הנוספת שהצפנת client-side הייתה נותנת (הגנה גם מפני מי שיש לו גישת אדמין/קונסולת Firebase) לא הצדיקה את המחיר: כל שיטה מעשית דורשת מפתח בצד הלקוח (סיסמה נפרדת או נגזרת מהתחברות) — וזה או שובר את ההתחברות עם Google (אין סיסמה לגזור ממנה), או יוצר איבוד מידע קבוע ובלתי הפיך אם המשתמש שוכח את המפתח. מאחר שאוכלוסיית היעד של האתר עשויה להיות בפרט פגיעה למצב כזה, וזהו פרויקט של מפתח יחיד ללא צוות אבטחה לניהול מפתחות כראוי — ההחלטה הייתה להימנע. **תחליף מומלץ במקום זאת:** לצמצם ככל האפשר מי שיש לו גישת אדמין/קונסולה לפרויקט ה-Firebase, ולשקול הפעלת Cloud Audit Logging על Firestore כבקרת גילוי (מי ניגש למה). יש לעדכן החלטה זו אם נוסף צוות, אם יש דרישת רגולציה מפורשת, או אם משתנה מודל האיום.

## מחיקה רכה ופח מחזור (30 יום)

כל כפתור מחיקה באתר — תוכנית, שיתוף עם נותן שירות, הערה, וכל ישות עתידית שתתווסף — מתנהג באופן אחיד:

- לחיצה על מחיקה **תמיד** פותחת את [ConfirmDialog.jsx](src/components/ui/ConfirmDialog.jsx) המשותף (לעולם לא `window.confirm`), עם ניסוח שמסביר שהפריט עובר לפח המחזור וניתן לשחזרו.
- אישור אינו מוחק את המסמך בפועל — הוא מסמן אותו כ"מחיקה רכה" (`deletedAt` = timestamp נוכחי) דרך [trash.js](src/services/trash.js) (`softDeleteDoc`). מחיקה קשה (`deleteDoc` אמיתי) אסורה גם ברמת [firestore.rules](firestore.rules) על מסמך שאין לו `deletedAt` מוגדר — כך שהדרך היחידה למחיקה קשה היא לעבור קודם דרך הפח.
- הפריט מוצג עד 30 יום ב"פח מחזור" — מקטע [TrashSection.jsx](src/components/ui/TrashSection.jsx) inline במסך הרלוונטי (Profile.jsx לתוכניות, Providers.jsx לשיתופים, CommentThread.jsx להערות) — עם אפשרות **שחזור** (`restoreDoc`, מנקה את `deletedAt`) או **מחיקה לצמיתות** (`deleteDoc` אמיתי, עם `ConfirmDialog` נוסף משלה כי זו הפעם היחידה שבאמת בלתי הפיכה).
- **אין Cloud Functions בפרויקט**, ולכן אין cron בצד שרת שמנקה פריטים אוטומטית אחרי 30 יום. הניקוי הוא "עצלן" (lazy) בצד לקוח: בכל טעינה של רשימת פריטים (`usePlans`/`useShares`/`useComments`), פריטים שחלפו עליהם 30 יום נמחקים ברקע (`trash.js`'s `purgeExpired`, best-effort, שגיאה בו לא מוצגת למשתמש כי זה ניקוי רקע ולא פעולה שהמשתמש ביקש).
- מסננים בין "פעיל" ל"בפח" (`splitByTrash`) ממומשים תמיד **בצד הלקוח** אחרי שליפת האוסף המלא, ולא ע"י `where("deletedAt", "==", null)` ב-Firestore — שדה חסר (מסמך שנוצר לפני התכונה) אינו שווה ל-`null` בשאילתת Firestore, כך שסינון שרתי כזה מסתיר בשקט מסמכים ישנים. חריג מתועד: [useSharedWithMe.js](src/services/useSharedWithMe.js) עדיין נשען על אותו עיקרון (סינון client-side), למרות ה-collection-group query.
- ביטול שיתוף חייב עדיין לחסום גישה **מיידית** (לא לחכות ל-30 יום) — ראו `shareActive()` ב-[firestore.rules](firestore.rules), שמתייחס לשיתוף עם `deletedAt` מוגדר כלא-פעיל לצורך `canAccessPlan`/`canEditPlan`, גם שהמסמך עצמו עוד קיים בפח.
- כל שינוי בחוקי Firestore הקשור לכך (מחיקה קשה מותנית ב-`deletedAt != null`, `shareActive`) חייב להיבדק מול האמולטור המקומי כמו כל שינוי חוק אחר — ראו [verify-firestore-rules.mjs](scripts/verify-firestore-rules.mjs) לתרחישי הבדיקה הקיימים (מחיקה רכה/שחזור/מחיקה קשה לכל אחת משלוש הישויות).
- אין להוסיף מנגנון מחיקה/פח מחזור נפרד לישות חדשה — כל תוספת עוברת דרך `trash.js`/`TrashSection.jsx` הקיימים.

## היסטוריית גרסאות

תוכן התוכנית (לא ההערות ולא השיתופים) עובר מעקב גרסאות, כדי שאפשר יהיה לראות מה השתנה ומתי — בפרט לזהות עריכה שקרתה שעות (או ימים) אחרי יצירת התוכנית:

- כל שמירה (כולל ה-autosave המבוצע ב-[FormPage.jsx](src/pages/FormPage.jsx)) עוברת דרך [savePlan.js](src/services/savePlan.js), שכותב, באותו `writeBatch` אטומי עם מסמך התוכנית עצמו, גם מסמך "גרסה" לתת-אוסף `users/{uid}/plans/{planId}/versions/{versionId}`: `data` (צילום שדות התוכן, ללא שדות מטא), `startedAt`/`updatedAt`, ו-`editedByUid`/`editedByName`/`editedByRole` (מי שמר לאחרונה).
- כדי שלא כל autosave (כל 2 שניות בזמן הקלדה) ייצור גרסה נפרדת, עריכות בטווח של פחות משעה (`VERSION_SESSION_GAP_MS` ב-savePlan.js) מתמזגות לאותה גרסה (Overwrite של `data`+`updatedAt`); רק פער של שעה ומעלה (או טעינת דף חדשה אחרי הפסקה ארוכה) פותח גרסה חדשה. ההחלטה "אותה גרסה או חדשה" מתבצעת מתוך שני שדות קלים שנשמרים על מסמך התוכנית עצמו — `versionSessionId`/`versionSessionAt` — כדי שלא תידרש קריאה נוספת ל-Firestore בכל שמירה (עקבי עם עקרון מינימום הקריאות למטה).
- קריאת ההיסטוריה ([usePlanVersions](src/services/planVersions.js), משמש את [VersionHistory.jsx](src/components/VersionHistory.jsx)) גלויה לכל מי שיש לו גישה לתוכנית — כולל נותן שירות בהרשאת צפייה-בלבד — בדיוק כמו הערות; כתיבה (כולל שחזור) מוגבלת למי שיש לו הרשאת עריכה (הבעלים או נותן שירות עם `permission: 'edit'`), כמו כל כתיבה אחרת לתוכן התוכנית.
- שחזור לגרסה ישנה ([VersionHistory.jsx](src/components/VersionHistory.jsx)) מחיל את השדות של הגרסה שנבחרה על הטופס וקורא ל-`savePlan` ישירות עם `forceNewVersion: true` — שחזור **תמיד** פותח גרסה חדשה משלו (מתויגת `revertedFromVersionId`), בלי קשר לחלון השעה, כדי שלא יימחק בשקט השינוי שהיה פתוח באותו רגע.
- אין מחיקה להיסטוריה — לא רכה ולא קשה — גם לא לבעל/ת התוכנית: ראו `allow delete: if false` בבלוק `versions` ב-[firestore.rules](firestore.rules). זו היסטוריה קבועה, לא ישות עם מחזור חיים כמו תוכנית/שיתוף/הערה, ולכן לא עוברת דרך `trash.js`.
- הצילומים כפופים לאותה החלטת הצפנה-at-rest המתועדת למטה (סעיף "אבטחה והצפנה") — הם לא חושפים יותר ממה שכבר חשוף במסמך התוכנית עצמו, ומוגנים באותם חוקי Firestore (גישה = גישה לתוכנית המקורית).
- תוויות השדות המוצגות בהשוואת הגרסאות (`FIELD_LABELS`-style מיפוי ב-[planVersions.js](src/services/planVersions.js)) הן מקור נפרד וממוקד לתצוגת ה-diff בלבד — לא קשור/כפול לטקסט ה-JSX הקיים בטפסים עצמם, ואינו צריך עדכון תואם אם טקסט תווית בטופס משתנה (רק אם מתווסף שדה חדש לגמרי שרוצים שיוצג יפה בהיסטוריה).

## מינימום קריאות לשרת ולבסיס הנתונים, יעילות

- כל קריאה ל-Firestore (או לכל שירות חיצוני) עולה כסף וזמן — יש לצמצם למינימום ההכרחי:
  - להימנע מקריאה חוזרת לאותם נתונים באותו טעינת עמוד/session; לשמור נתונים שכבר נטענו ב-state/context (למשל [AuthContext.jsx](src/context/AuthContext.jsx)) ולהשתמש בהם מחדש במקום לשלוף שוב.
  - שמירה אוטומטית (ראו המימוש ב-[FormPage.jsx](src/pages/FormPage.jsx)) חייבת: (א) לפעול עם debounce, לא לשלוח כתיבה על כל הקלדה; (ב) להיבדק מול `formState.isDirty` של react-hook-form לפני שמירה — כדי שפתיחת תוכנית קיימת לצפייה בלבד, ללא שום עריכה, לא תגרום לכתיבה מיותרת; (ג) להיות עטופה ב-try/catch עם הודעת שגיאה למשתמש (toast) — כשל שקט בשמירה אוטומטית הוא חמור, כי המשתמש חושב שהמידע נשמר.
  - יצירת ישות חדשה (תוכנית, מסמך) לא צריכה לגרום לכתיבה ריקה ל-Firestore לפני שהמשתמש הזין תוכן בפועל — מספיק ליצור מזהה (uuid) בצד הלקוח ולתת לשמירה הראשונה (האוטומטית או הידנית) ליצור את המסמך.
  - כשעוברים בין מסכים עם נתונים שכבר נשלפו (למשל מרשימת תוכניות ב-[Profile.jsx](src/pages/Profile.jsx) אל [FormPage.jsx](src/pages/FormPage.jsx)) — יש להעביר את הנתונים דרך ה-state של הניווט (`navigate(path, { state })`) במקום לבצע קריאה חוזרת לאותו מסמך.
  - לשלוף רק את השדות/מסמכים שבאמת נדרשים למסך הנוכחי, לא מסמכים שלמים "ליתר ביטחון".
  - לפני הוספת קריאה חדשה לשרת, יש לבדוק אם ניתן להסתפק בנתון שכבר קיים בזיכרון/state.
- באופן כללי, כל קוד חדש (רינדור, לולאות, טיפול בטפסים) ייכתב מתוך מחשבה על יעילות — להימנע מחישובים/רינדורים מיותרים וממבני נתונים לא יעילים, ולא רק "לגרום לזה לעבוד".

## חוויית משתמש נקייה ואינטואיטיבית

- כל מסך/זרימה חדשים חייבים להיות ברורים מאליהם למשתמש ללא צורך בהסבר: פעולה עיקרית בולטת אחת למסך, ניווח/כותרות ברורים, ומצבי טעינה/שגיאה/הצלחה מפורשים (למשל דרך react-toastify הקיים) ולא שקטים.
- להעדיף פשטות על פני ריבוי אפשרויות: אם ניתן לצמצם שדות/צעדים בטופס מבלי לפגוע בתפקוד — לצמצם.
- כל פעולה הרסנית או בלתי הפיכה (מחיקת תוכנית, יציאה מהחשבון) חייבת אישור מפורש מהמשתמש לפני ביצוע.
- לשמור על עקביות בין מסכים (מיקום כפתורים, ניסוח, מבנה טופס) כדי שהמשתמש לא יצטרך ללמוד מחדש כל מסך.

## שמירה על README.md מעודכן

- [README.md](README.md) חייב לשקף תמיד את המצב האמיתי של הפרויקט: הטכנולוגיות בפועל (כרגע Tailwind, לא Bootstrap), שלבי ההתקנה בפועל (כולל שימוש ב-`.env`/`.env.example`), והפקודות הזמינות ב-`package.json`.
- כל שינוי שמשנה את אחד מאלה — הוספת/החלפת ספרייה מרכזית, שינוי בתהליך ההתקנה או משתני הסביבה הנדרשים, הוספת פקודת `npm` חדשה — חייב לכלול עדכון תואם ל-README.md כחלק מאותו commit, לא כמשימה נפרדת בהמשך.
- אין להשאיר ב-README.md תיעוד של טכנולוגיה/תהליך שכבר הוחלף או הוסר בפועל.

## עדכון קובץ זה

כשמתבצע שינוי גדול באתר — מעבר לספריית עיצוב/אייקונים אחרת, שינוי במבנה הנתונים ב-Firestore, הוספת מנגנון אימות/הרשאות, שינוי בארכיטקטורת התיקיות — יש לעדכן את הסעיף הרלוונטי במסמך הזה **וגם** את README.md, כחלק מאותו שינוי, לא בנפרד.
